# Copy this file to .env and fill in your values
# The bot reads from .env automatically

# Optional: override the config.json group name via env var
# WHATSAPP_GROUP_NAME="Bella 🐾"

# Optional: set log level (info | debug)
LOG_LEVEL=info
